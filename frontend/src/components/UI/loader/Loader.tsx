import { FC } from 'react';

import './Loader.css';

export const Loader: FC = () => {
    return (
        <div className="loader-container">
            <div className='loader' />
        </div>
    );
};